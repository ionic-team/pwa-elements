import { Component, Prop } from '@stencil/core';

@Component({
  tag: 'ion-camera',
  styleUrl: 'camera.scss',
  shadow: true
})
export class Camera {
  render() {
    return (
      <div>
      </div>
    );
  }
}
